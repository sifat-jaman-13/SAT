name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup MSYS2 (gcc)
        uses: msys2/setup-msys2@v2
        with:
          update: true
          install: >-
            mingw-w64-x86_64-gcc

      - name: Build Phonebook (Production)
        shell: msys2 {0}
        run: gcc -Wall -Wextra -O2 "Phonebook_FIXED.c" -o phonebook.exe

      - name: Build Movie Management System (Production)
        shell: msys2 {0}
        run: gcc -Wall -Wextra -O2 "Movie_Management_System_FIXED.c" -o movie_management.exe
